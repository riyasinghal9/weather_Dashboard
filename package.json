{
  "name": "weather-dashboard",
  "version": "1.0.0",
  "description": "A comprehensive weather dashboard with current weather and 5-day forecasts for multiple cities",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "start": "cd backend && NODE_ENV=production npm start",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "workspaces": [
    "frontend",
    "backend"
  ],
  "keywords": ["weather", "dashboard", "react", "express", "forecast"],
  "author": "Weather Dashboard Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
} 